# (BYOL) 라이선스

라이선스 재구축과 갱신을 통해 BYOL 방식으로 운영 중인 DB 서비스의 라이선스 구성을 유지하고 교체합니다.

**재구축**은 라이선스 구성이 초기 설정에 미달된 상태일 때 부족한 인스턴스를 추가 생성하여 원래 구성으로 복구하는 기능입니다. **라이선스 갱신**은 만료 90일 이전부터 새 라이선스 파일을 업로드하여 라이선스를 교체하는 기능으로, BYOL 라이선스에만 적용됩니다. 두 기능 모두 Root 및 Member 권한 사용자가 **관리 > Overview > 라이선스** 탭에서 수행할 수 있습니다.

{% hint style="info" %} **참고**
**지원 환경** AWS 환경에서는 Tibero BYOL 라이선스에 한해 두 기능을 사용할 수 있습니다. Azure 환경에서는 Tibero 및 OpenSQL 모두 지원합니다. {% endhint %}

## 라이선스 재구축

라이선스 구성이 미달 상태일 때, 부족한 인스턴스를 추가 생성하여 원래 구성으로 복구합니다. 유휴 라이선스가 존재하는 경우에만 재구축을 수행할 수 있습니다.


1. **관리 > Overview**에서 **라이선스** 탭을 클릭합니다.
2. **재구축** 버튼을 클릭합니다.
3. 확인 모달에서 현재 운영 중인 인스턴스 수와 재구축 후 최종 구성을 비교합니다.
4. **확인** 버튼을 클릭합니다.

### 라이선스 타입 별 재구축 동작

| 라이선스 타입 | 복구 내용 |
|---------|-------|
| Single | Standby 인스턴스를 추가 생성하여 Recovery Standby로 할당합니다. |
| TSC | Standby 인스턴스를 추가 생성하여 Read Only Standby로 할당합니다. |
| TAC | 초기 구성과 Primary 노드 수가 일치하지 않으면 Primary로, 일치하면 Standby로 추가 생성하여 할당합니다. |

{% hint style="info" %}**참고**
재구축이 진행되는 동안 서비스 상태는 `updating`으로 표시됩니다. 재구축 결과는 인스턴스 단위로 결정되며, 일부 인스턴스가 실패하더라도 재구축 버튼을 통해 다시 시도할 수 있습니다. {% endhint %}

## 라이선스 갱신

BYOL 라이선스의 만료일이 90일 이내로 남으면 새 라이선스 파일을 업로드하여 갱신할 수 있습니다. 만료일이 90일 이내로 남으면 Overview 페이지에 만료 안내 배너가 표시됩니다. 배너의 **갱신** 링크 또는 **라이선스 갱신** 버튼을 클릭하면 갱신 화면이 열립니다. 갱신 화면에는 서비스 이름, DB 엔진, 라이선스 옵션, 토폴로지, 노드 수 등 기본 정보가 조회 전용으로 표시됩니다.


1. **작업** 버튼을 클릭합니다.
2. **라이선스 갱신** 버튼을 클릭합니다.
3. **올리기** 버튼을 클릭하거나 파일을 드래그 앤 드롭하여 라이선스 파일을 업로드합니다.
4. 검증할 파일을 **체크**하고, **검증** 버튼을 클릭하여 라이선스 파일의 유효성을 확인합니다.
5. 검증 성공 후 **갱신** 버튼을 클릭합니다.

### 라이선스 파일 업로드 및 검증

**올리기** 버튼을 클릭하거나 파일을 드래그 앤 드롭하여 라이선스 파일을 업로드합니다. 업로드한 파일은 목록에서 다음 정보를 확인할 수 있습니다.

| 항목 | 설명 |
|-----|-----|
| 라이선스 파일 | 파일명 |
| Edition | 에디션 정보 |
| CSP | CSP 정보 |
| Limit CPU | 라이선스에서 허용하는 vCPU 수 |
| Expired Date | 라이선스 만료일 |
| Signature | 시그니처 정보 |
